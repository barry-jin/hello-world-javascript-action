on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world action step
        uses: ./ # Uses an action in the root directory
        id: hello
        with:
          profile-name: null
          region: 
          name: 
          job-type: 
          source-ref: 
          work-dir: 
          saved-output: 
          save-path: 
          command: 
          remote: 
          wait: 
          time-out:   
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The batch status was ${{ steps.hello.outputs.batch-status }}"
  unittest:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world action step
        uses: ./ # Uses an action in the root directory
        id: hello
        with:
          profile-name: null
          region: 
          name: 
          job-type: 
          source-ref: 
          work-dir: 
          saved-output: 
          save-path: 
          command: 
          remote: 
          wait: 
          time-out:   
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The batch status was ${{ steps.hello.outputs.batch-status }}"